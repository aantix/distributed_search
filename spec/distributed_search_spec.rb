require 'spec_helper'

describe DistributedSearch do
  it 'has a version number' do
    expect(DistributedSearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
